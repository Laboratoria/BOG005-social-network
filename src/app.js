import { login, logout } from "./auth.js";
const buttonLogin = document.querySelector(".googleButton");

console.log(buttonLogin);

buttonLogin.addEventListener("click", (e) => {
  console.log("called   ");
  try {
    // await;
  } catch (error) {}
});
